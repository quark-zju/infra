all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
    macos:
      hosts:
        localhost:
    linux:
      hosts:
        x13:
        sf:
          ansible_user: root
        hz:
        opi:
    opi_nodes:
      hosts:
        opi:
          ansible_user: root
